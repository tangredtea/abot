// Package mcp implements an MCP (Model Context Protocol) client that connects
// to stdio or HTTP MCP servers, discovers tools via JSON-RPC 2.0, and wraps
// each remote tool as an ADK tool.Tool.
package mcp
