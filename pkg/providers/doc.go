// Package providers contains LLM provider implementations and the provider
// registry. Sub-packages: anthropic (native API + prompt caching),
// openaicompat (ChatCompletion compatible + reasoning model), fallback (chain
// with cooldown and error classification).
package providers
